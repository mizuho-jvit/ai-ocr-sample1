# リポジトリ運用ガイドライン

## プロジェクト構成とモジュール配置

このリポジトリは最小構成のOCRデモ用スキャフォールドです。プロジェクト概要は `README.md` に記載します。入力資料・画像は `input/`、OCRの生成物は `output/` に置き、どちらもGit管理の対象外とします。顧客資料、抽出テキスト、その他の生成データはコミットしないでください。実装を追加する際は、アプリケーションコードを `src/`、テストを `test/`（またはモジュールの隣に `*.test.*`）に置き、再利用可能で機微情報を含まないフィクスチャは `test/fixtures/` に置きます。

### Cloudflare Workers の公開境界

- Workerのアプリケーションコードは `src/` に置きます。
- デプロイ可能な静的アセットは `public/`、または `dist/` などの専用ビルド成果物ディレクトリにのみ置きます。
- 要件・設計資料は `knowledge/`、実装メモは `docs/` に置きます。これらのディレクトリをWorkerのアセット配信元にしてはいけません。
- Wranglerの `assets.directory` に、リポジトリ直下、`knowledge/`、`docs/` を指定してはいけません。
- 明示的に公開承認されていない限り、Workerコードから `knowledge/` または `docs/` の文書をインポート・配信してはいけません。

## ビルド・テスト・開発コマンド

現時点では、パッケージマニフェスト、ビルドシステム、実行可能ファイル、自動テストコマンドはコミットされていません。コードを提出する前に、次を実行してリポジトリを確認してください。

```sh
git status --short
git diff --check
```

最初のコマンドは意図しない変更・未追跡ファイルを検出し、次のコマンドは空白エラーを検出します。ランタイムやビルドツールを導入する場合は、ロックファイルを追加し、セットアップ手順と代表的なコマンドを `README.md` に記載してください。該当する場合は `npm run build`、`npm test`、`npm run lint` のように予測しやすいスクリプトを提供します。

## コーディングスタイルと命名規則

追加する言語用に設定されたフォーマッターとリンターに従い、最初の関連ソースファイルとともに設定ファイルもコミットします。それまでは、UTF-8テキスト、JSON/YAMLでは2スペースインデント、ソースコードでは各エコシステムの標準に従います。ファイル名は説明的な小文字・ハイフン区切り（例：`ocr-client.ts`）を優先します。JavaScriptまたはTypeScriptでは、変数・関数に `camelCase`、クラス・型に `PascalCase`、定数に `UPPER_SNAKE_CASE` を使用します。

## テスト方針

振る舞いを変更するたびにテストを追加してください。特に不正な入力、未対応形式、OCR結果が空の場合、プロバイダー障害を対象とします。フィクスチャは小さく、合成データのみとし、秘密情報や個人情報を含めません。テスト名は `ocr-client.test.ts` のように対象の振る舞いが分かるものにします。カバレッジツールを導入する場合は、生成される `coverage/` をGit管理対象外にしてください。

## コミットとプルリクエストの方針

履歴には現在 `Initial commit` しかないため、確立済みの慣習はありません。`Add OCR response parser` のように、短く命令形のコミット件名を使い、無関係な変更を同じコミットに混ぜないでください。プルリクエストには、目的、実装内容、実施した検証、設定変更を説明します。関連Issueをリンクし、OCRの観測可能な振る舞いが変わる場合は、匿名化済みの出力例またはスクリーンショットを添付してください。

## セキュリティと設定

APIキーはGit管理対象外の `.env` ファイルに置き、安全なプレースホルダーを `.env.example` に用意します。認証情報をログに出力したり、実際の入力・出力資料をコミットしたりしてはいけません。push前に `git diff --cached` でステージ済みファイルを確認してください。
