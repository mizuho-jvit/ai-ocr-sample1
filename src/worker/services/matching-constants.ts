/**
 * 🔵 Intent: NF-4-3「審査ルール・確信度閾値・名寄せスコアリングの重みは定数として一元管理し、
 * ロジック内に散在させない」に従い、F-6（名寄せ）まわりで要件に明記が無く実装時に判断した
 * 定数（決定#22）だけをこのファイルへ集約する。営業デモの運用で調整が要りそうな値
 * （スコアの重み）は、ここだけを直せば済むようにする。OCRラベルの表記ゆれ辞書は
 * `../../shared/field-label-aliases.ts`（決定#26・#38。SPAの候補カードとも共有するため）。
 */

/**
 * 🔴 Intent: F-6-3は条件の分類（高スコア2種・中スコア1種）のみを定め、具体的な重みは
 * 要件に明記がない。高スコア2条件を同じ重みにする（決定#22）。
 */
export const SCORE_WEIGHTS = {
  kanaAndBirthDate: 60,
  name: 30,
  phone: 60,
} as const;
